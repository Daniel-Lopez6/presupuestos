/* =========================================================================
   config.js — la única pieza que hay que rellenar a mano

   clienteGoogle: el ID de cliente OAuth que crearás en Google Cloud para
   poder sincronizar los datos con Google Drive. No es una contraseña ni un
   secreto: viaja dentro de la página y Google solo lo acepta desde las
   direcciones que tú autorices en la consola.

   Déjalo vacío y la aplicación funciona igual, sin sincronización.
   Los pasos exactos están en el README, apartado "Sincronización".
   ========================================================================= */
window.CONFIG = {
  clienteGoogle: ''
};
